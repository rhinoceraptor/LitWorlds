# Jasmine Unit Testing, with CoffeeScript

#### A simple RPN calculator in client side coffeescript. The calculator logic is validated in Jasmine, the interface is also written in coffeescript using Jquery.